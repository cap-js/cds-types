// verifies typical legacy import patterns
// formerly through the legacy facade in @sap/cds/apis/...
// now through official cds facade

import * as cds from '@sap/cds' // '@sap/cds/apis/cds'

import { Association, entity } from '@sap/cds' // '@sap/cds/apis/core'
import { CREATE, SELECT, Query } from '@sap/cds' // '@sap/cds/apis/cqn'
import {
  Association as CsnAssoc,
  Definition,
  entity as csnEntity,
  struct,
  type,
  Element,
  CSN,
} from '@sap/cds' // '@sap/cds/apis/csn'
import { Request, User } from '@sap/cds' // '@sap/cds/apis/events'
import {
  ArrayConstructable,
  Constructable,
} from '@sap/cds' // '@sap/cds/apis/internal/inference'
import { LinkedDefinition } from '@sap/cds' // '@sap/cds/apis/linked'
import { ConstructedQuery } from '@sap/cds' // '@sap/cds/apis/ql'
import {
  ApplicationService,
  DatabaseService,
  QueryAPI,
  Transaction,
  Service,
} from '@sap/cds' // '@sap/cds/apis/services'

import {
  Definition as DefinitionRefl,
  entity as entityRefl,
  LinkedModel,
  LinkedDefinition as LinkedDefinitionRefl,
  ParsedModel,
  ReflectedModel,
  ReflectedDefinitions,
} from '@sap/cds' // '@sap/cds/apis/reflect'

// removed, as is it no longer compatible with latest version of cds-types
//import { Definitions } from '@sap/cds/apis/csn'
import { User as CoreUser } from '@sap/cds' // '@sap/cds/apis/core'
import { Logger } from '@sap/cds' // '@sap/cds/apis/log'